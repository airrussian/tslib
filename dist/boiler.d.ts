export declare class Boiler {
    protected type: number;
    protected serial: number;
    protected password: number;
    constructor(type: number, serial: number, password: number);
    getId(): string;
}
